# Frontend Environment Variables
# Copy to .env.local and fill in your values. Never commit .env.local.

# Backend API base URL — change this for production deployment
REACT_APP_API_URL=http://localhost:5000

# Cloudflare Turnstile site key (CAPTCHA)
# FIX #2: Move the hardcoded key out of source code into env
REACT_APP_TURNSTILE_SITE_KEY=0x4AAAAAAAU56i0A4rZ8Qv6i
