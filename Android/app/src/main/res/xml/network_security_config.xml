<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration
    Certificate pinning: Only trust *.supabase.co certificates.
    This prevents Man-in-the-Middle (MITM) attacks by proxy tools.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Permit cleartext for local development (debug only) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
